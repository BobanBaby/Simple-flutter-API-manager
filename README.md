# Flutter network call Manager

A Simple class to handle api requests in flutter

## Advantages

- Using only http plugin
- Cookie support using cookie jar
- null safty
- SSL pinning support (bin folder included to generate the required data)